# Copy to .env and fill in. Never commit a secret key.

# Alias of the funded Testnet identity used to deploy. Create one with:
#   stellar keys generate cygnus-deployer --network testnet --fund
SOURCE=cygnus-deployer

# Network to deploy against. Testnet only; there is no mainnet configuration.
NETWORK=testnet
